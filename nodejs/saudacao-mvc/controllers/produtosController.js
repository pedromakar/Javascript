module.exports = {

    lista: (req, res) => {
        res.send('Lista de produtos (controller padronizado)');
    },

    cadastrar: (req, res) => {
        res.send('Produto cadastrado (controller padronizado)');
    }

};
